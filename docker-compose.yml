services:
  backend:
    container_name: api-tsc
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - /opt/nodeapps/uploads:/opt/nodeapps/uploads
    restart: always


  # mongo:
  #   image: mongo:6
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo_data:/data/db
  #     - /opt/nodeapps/uploads:/opt/nodeapps/uploads

# volumes:
#   mongo_data:
